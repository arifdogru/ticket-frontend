{
  "name": "ticket-frontend",
  "version": "1.0.0",
  "engines": {
    "node": "12.13.0"
  },
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "bootstrap": "^4.3.1",
    "react": "^16.11.0",
    "react-bootstrap": "^1.0.0-beta.14",
    "react-dom": "^16.11.0",
    "react-scripts": "3.2.0",
    "svgo": "1.3.0"
  },
  "scripts": {
    "cross:env": "npm i -g cross-env",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "prod": "cross-env NODE_ENV=production serve -s build -p 4200",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cross-env": "^5.2.0"
  }
}
